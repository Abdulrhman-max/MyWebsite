import { PriceTagPermission } from './price-tag-permission';

describe('PriceTagPermission', () => {
  it('should create an instance', () => {
    expect(new PriceTagPermission()).toBeTruthy();
  });
});
